'use client';

import React from 'react';

/**
 * Protocol Retired: System now allows direct dashboard access.
 */
export function FullscreenLock({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
